# Field report — PR 5 actions composées

Exécution contrôlée le 17 septembre 2026 sur le Chrome du propriétaire exposé en CDP 9223. Aucun titre, nom, message, ref applicative, identifiant de conversation ou contenu privé n'est consigné.

## Résultats

| Scénario | Résultat |
|---|---|
| S3 messagerie, lecture seule | batch `find + text`, puis observation finale : 2 étapes réussies, 324 ms, 9 695 octets |
| S5 recherche | la cible sémantique localisée fonctionne ; `type --press Enter --wait-url` réussit en une action batch |
| nettoyage S5 | deuxième action batch avec texte vide et `clear: true` ; `verifiedValue` vide ; retour `/feed/` |
| `navigate --observe` | succès live en 5 203 ms, 666 octets, avec `scope` et arbre présents ; la fixture couvre aussi un landmark `main` ajouté avec retard |

Le scénario S3 est resté en lecture seule : la conversation ouverte n'exposait pas de compositeur de message identifiable comme `contenteditable`, donc aucune saisie n'a été tentée. Cette décision évite de confondre la recherche de conversation avec un compositeur.

Sur S5, une observation indépendante `--within main` lancée dès que l'URL de recherche change peut précéder le montage du landmark et retourner `TARGET_MISSING`. La saisie, `Enter`, la transition d'URL et le nettoyage ont réussi. `navigate --observe` possède désormais sa propre attente bornée du scope, couverte par une fixture locale déterministe et validée en live.

## Hygiène et état final

- Aucun message, publication, like, connexion, follow, acceptation, suppression, upload, paiement ou réglage.
- Aucun bouton irréversible n'a été cliqué et aucun token de confirmation n'a été consommé.
- Le champ de recherche a été vidé et sa valeur vide vérifiée par le résultat de saisie.
- L'onglet LinkedIn est revenu sur `/feed/`.
- Aucun onglet utilisateur n'a été fermé.
- Le daemon de test a été arrêté.
